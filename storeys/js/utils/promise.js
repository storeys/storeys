define(
    ['../../../storeys/lib/zousan'],
    function(zousan) {
      // note: AMD lines in Zousan code is commented out
      // Export the global variable of Zousan
      return Zousan;
    }
);
